apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: voidpulse
  labels:
    app: backend
    app.kubernetes.io/name: voidpulse
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
